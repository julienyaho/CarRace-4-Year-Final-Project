using UnityEngine;
using System.Collections;

public class Sound : MonoBehaviour 
{
	/*void OnCollisionExit(Collision col)
	{
		Debug.Log (gameObject.name = " has collided with wall" = col.gameObject.name);
	}*/
	/*void OnCollisionStay(Collision col)
	{
		if (!audio.UnityEngine.Component && col.relativeVelocity.magnitude >= 2) {
			audio.volume = col.relativeVelocity.magnitude / 5;
			audio.play ();
		}
	}*/

}
